import { RainbowTypingDirective } from './rainbow-typing.directive';

describe('RainbowTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowTypingDirective();
    expect(directive).toBeTruthy();
  });
});
